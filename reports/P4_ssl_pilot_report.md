# P4 — CIFAR-10 SSL development pilot report (VCS-QMI, SimCLR-matched, VICReg-matched-128)

Status: **DRAFT — sections C–E are filled only after all three P3 runs and their frozen-feature evaluations finish.**
Neutral results table: `reports/P4_ssl_pilot_results_table.md` (generated by `vcs_ssl.summarize`, failed runs included).
Pre-registration: `reports/P3_PILOT_PREREG_FROZEN_20260924.md`.  Contract: `VCS_QMI_SSL_Server_Agent_Spec_v1.md`.

## A. Implementation check

| item | value |
|---|---|
| repo / commits | `/home/infres/yinwang/CS_QMI/ssl_pilot`, branch `main`; code commit for P1–P3: `76cebd6` (src identical in `57ac0c8`/`163a912`, which only add reports) |
| objective | **original formula** via `reference/ssl_core.py` (sha256 `580ac2be…3283d8`, byte-identical to the starter): `J = mean(t_pos) − mean(t_neg) − ½mean(t_pos²) − ½mean(t_neg²)`, `loss = −J`, `R_binary = 1 − J`; no CS transform, no clipping, no extra terms |
| update rule | ordinary joint update: one `loss.backward()` updates encoder + projector + critic through the same AdamW; no min-max, no critic warm-up, no extra critic steps, no stop-gradient, no EMA |
| encoder | `torchvision.models.resnet18(weights=None)` with CIFAR stem (3×3 s1 conv1, no maxpool, `fc=Identity`), 11,168,832 params, `h ∈ R^512` |
| projector | `Linear(512,512,bias=False) → BN → ReLU → Linear(512,128)`, 328,832 params; `z = normalize(p_raw, eps=1e-8)` for VCS/SimCLR; raw `p_raw` for VICReg |
| critic (VCS only) | reference `PairCritic`: ordered concat (256) → 512 → ReLU → 512 → ReLU → 1 → tanh; no BN/dropout; last layer Xavier-uniform gain 0.1, bias 0; 394,753 params; single critic object for positive and negative pairs |
| negatives | K=1 random nonzero cyclic shift per step from a dedicated CPU `torch.Generator` (seed `seed+100003`), B negatives per step, no detach, no label filtering, no queue |
| controls | SimCLR NT-Xent τ=0.2 on 2B L2-normalized views (self masked, positive kept in denominator); VICReg (25/25/1, eps 1e-4, ddof 1) on raw `p_raw`; no critic object exists in either run |
| extra losses | none (`extra_regularizers=[]` enforced by config policy checks) |
| data | official CIFAR-10 train batches (md5 verified against the CIFAR page) taken from the user's existing copy `/projects/EEG-foundation-model/yinghao/FMCA-AV/cifar10` via symlink; the official test batch is never read (loader refuses `train=False`; verified by a spy test). Split `dev45k_val5k`, seed 20260924, 500/class selection; manifest sha256 `c35d7cd336e79680c2e2c077e5612ed9eb6779ff70a427f5e085bc4df2d617a9`, shared by all runs |
| engineering choices (not collaborator defaults) | all of spec §8 (AdamW 1e-3, wd 1e-4 matrices / 0 bias-BN-critic, 2-epoch linear warm-up → cosine to 1%, B=256, FP32, TF32 off, cudnn.benchmark off, 4 workers); smoke horizon/warm-up scaling; critic init stream `seed·1000003+7919`; loader generator seed = `seed`; in-training diagnostics on a fresh checkpoint copy under `fork_rng`; epoch-boundary-only resume |
| differences vs collaborator code | no collaborator training code exists on the server; only the starter reference was available and is used unmodified. An unrelated SSL harness (`~/FMCA-AV`) exists; nothing from it except the raw CIFAR files is reused |
| hardware / software | P0–P2 on RTX PRO 6000 Blackwell (node60), P3 on A100-PCIE-40GB (node54, see table C for the others); Python 3.12.12, torch 2.9.0+cu128, torchvision 0.24.0, cuDNN 91002; single GPU; all jobs via SLURM (ids in `reports/job_ids.json`) |

## B. Tests

| check | result |
|---|---|
| 29 reference tests (`tests/test_ssl_core.py`, unchanged) | **29/29 passed** on the server GPU node (job 1007755) and on a CPU node (job 1007742) |
| 1 real ResNet shapes (h 512 / p 128 / z 128, critic B and K·B outputs) | PASS |
| 2 real gradients & optimizer coverage (every trainable param exactly once; encoder/projector/critic non-zero grads; both views and the negative branch reach the encoder) | PASS for vcs, simclr, vicreg |
| 3 labels inaccessible (SSL dataset returns `(v1, v2, uid)` only; permuting class labels leaves losses and shifts identical) | PASS |
| 4 split isolation (fit ∩ selection = ∅; batches with a selection UID or duplicate UID are refused; real manifest 45,000/5,000 with 4,500/500 per class) | PASS |
| 5 augmentation independence (two views of one image differ; two calls differ; clean transform deterministic) | PASS |
| 6 evaluation does not pollute training (encoder params + BN buffers hash-identical after probe; in-training kNN/spectrum/critic diagnostics leave all six training RNG streams untouched) | PASS |
| 7 checkpoint resume (CPU, single worker: 6 continuous steps == 3 steps + resume, bit-identical params, losses and shifts; horizon change refused) | PASS — **bit-exact only claimed for CPU/single-worker**; on GPU (P2) resume reproduced a full trajectory but not bit-identically (see D) |
| 8 config strictness (unknown field, missing field, K≠1, extra regularizers, negative detach, pretrained weights, test access, non-FP32, clipped J, unresolved `${…}` → all refused) | PASS |
| 9 no official test access (`train=False` raises; spy on `torchvision.datasets.CIFAR10.__init__` sees only `train=True` during the real load) | PASS |
| 10 controls train and emit epoch-0 / final artifacts; VCS emits J/R/shift stats, critic checkpoint state, `best_metric_policy=None` | PASS |
| numerical failure path (NaN critic weight → `FAILED_NUMERICAL`, failure artifacts written, no batch skipping) | PASS |
| P2 smoke gate (3 × 100 real-data steps; first-step gradient PASS all modules; finite; J∈[−3,1], J=1−R; RNG isolation; resume trajectory) | **GATE PASS** (`reports/P2_smoke_gate.md`, job 1007756) |

Data-leak evidence: manifest UID sets are disjoint and cover exactly 0..49,999; the trainer asserts every batch ⊂ fit; the test batch is never instantiated.
Two-view gradients: `z_l2.grad` non-zero on both halves; negative-branch-only backward reaches the encoder.
Checkpoint recovery level: **epoch boundary** (`last.pt` atomic write; mid-epoch progress discarded and recorded).

## C. Run table
_(pending — filled from `P4_ssl_pilot_results_table.md` after P3 completes)_

## D. Observations
_(pending)_

## E. Next step
_(pending — one prioritized option, then wait for the owner's decision)_
