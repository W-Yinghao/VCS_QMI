status: PLANNED_NOT_AUTHORIZED_BY_THIS_STARTER
dataset: cifar10
dev_epochs: 200
warmup_epochs: 10
seed_list:
- 0
- 1
- 2
restart_from_initial_weights: true
requires:
- P3_report_review
- frozen_selection_rule
- baseline_tuning_budget
- resource_approval
official_50k_retrain_and_10k_test: Separate confirmation protocol after recipe lock.
